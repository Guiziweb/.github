# Docker

- Toujours passer par `make` pour exécuter des commandes PHP, Composer ou Symfony Console.
- Ne jamais exécuter ces commandes directement sur la machine hôte.
- Toujours préciser `ENV=dev` en local.